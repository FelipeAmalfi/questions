import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

    searchBox: {
        margin: 8
    },

    icon: {
        marginLeft: 8
    },
    autocompleteContainer: {
        flex: 1,
        left: 0,
        position: 'absolute',
        right: 0,
        top: 100,
        zIndex: 1
    }
});
