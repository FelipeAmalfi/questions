import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        justifyContent: "flex-end",
        margin: 0,
    },
    visibleContainer: {
        backgroundColor: '#fff',
        borderColor: '#000000',
        height: 120,
        padding: 20,
        borderWidth: 1,

    }
})